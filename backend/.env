# ── LOCAL DEV — Hasker & Co. Realty Group ──────────────────────────────────────
# Production credentials live on the server as real env vars (not this file).
# This file is only used locally. Never commit it.

# Django
SECRET_KEY=zJs-x654D3NSgECOfRFR5SqoG4WsmVxNYMdIJLcvK1JFTAVa3lgwEkpWibg-G7-D4ns
DEBUG=True
DJANGO_SETTINGS_MODULE=config.settings.local
ALLOWED_HOSTS=*

# Database — SQLite for local dev (production DB stays on the server)
DATABASE_URL=sqlite:///db.sqlite3

# CORS — allow local frontend (local.py also sets CORS_ALLOW_ALL_ORIGINS=True)
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3003
FRONTEND_URL=http://localhost:3000

# Cloudinary (same account for dev + prod)
CLOUDINARY_CLOUD_NAME=da8i2waxr
CLOUDINARY_API_KEY=359642287496243
CLOUDINARY_API_SECRET=hvlqd1JuRWDLX41E0XXKH6sp0q0

# Email — print to console in local dev (local.py overrides the backend)
EMAIL_HOST=smtp.gmail.com
EMAIL_PORT=587
EMAIL_HOST_USER=
EMAIL_HOST_PASSWORD=
DEFAULT_FROM_EMAIL=Hasker & Co. Realty Group <info@haskerrealtygroup.com>


# Redis — leave blank locally (Celery tasks are skipped gracefully)
REDIS_URL=redis://:qwjiBno7nAJiJdJW4ec@127.0.0.1:35717/0

# Sentry — leave blank locally
SENTRY_DSN=
